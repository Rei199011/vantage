name: Radar

on:
  schedule:
    # 05:40 UTC, de lunes a viernes: antes de que abra Europa.
    - cron: '40 5 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: vantage
  cancel-in-progress: false

jobs:
  escanear:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Bajar el repo
        uses: actions/checkout@v4

      - name: Preparar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Escanear el universo
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL }}
        run: python bot.py --radar

      - name: Publicar el ranking
        run: |
          git config user.name  "Vantage"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add radar.json watchlist_auto.json
          git diff --staged --quiet && echo "Sin cambios" && exit 0
          git commit -m "Radar — $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
