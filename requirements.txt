# Vantage — dependencias
# Instalar con: pip install -r requirements.txt

pandas>=2.0
python-dotenv>=1.0

# Datos de mercado: divisas, metales, índices y acciones.
yfinance>=0.2.40

# El extra [job-queue] es obligatorio: sin él no existe app.job_queue
# y el modo --daemon no puede programar las revisiones periódicas.
python-telegram-bot[job-queue]>=20.0

# Opcional: revisión con IA (editorial del panel + análisis de los candidatos).
# Sin esto Vantage funciona igual, con el resumen generado por reglas.
# La clave se saca gratis en https://aistudio.google.com
google-genai>=1.0
